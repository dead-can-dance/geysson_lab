# Datos de muestra — NO son contenido final

`productos.json` y `testimonios.json` son datos placeholder tomados del
prototipo aprobado (`Neurocebryl Sitio.dc.html`) y de
`muestra_catalogo_neurocebryl.csv`. Sirven para construir y probar la
estructura del sitio (componentes, rutas, filtros).

**Regla del brief (PROJECT_BRIEF.md, sección 7):** a la fecha, 0 de 20
productos y 0 de 74 claims están aprobados en el PIM del cliente. Ningún
beneficio, descripción o claim de este archivo debe pasar a producción
tal cual. Cuando el cliente apruebe fichas en el PIM, regenerar este JSON
desde la hoja "07 Export web" y reemplazar por completo estos datos —
no editar producto por producto a mano.
